# Agent 지침 — FindIt AI 프로젝트

이 문서는 [prd.md](./prd.md)를 기반으로 코딩 에이전트(AI)가 이 프로젝트를 개발할 때 따라야 할 규칙입니다.
바이브코딩(빠르게 만들고 검증) 방식으로 진행하되, 아래 원칙과 범위를 반드시 지켜주세요.

---

## 1. 프로젝트 한 줄 요약
자연어로 애매하게 상품을 설명하면 AI가 의도를 분석해 상품과 최저가 구매처를 추천해주는 검색 서비스. 상세 요구사항은 반드시 [prd.md](./prd.md)를 기준으로 판단할 것.

---

## 2. 현재 단계: MVP (Phase 1)
이번 단계부터 **실제 외부 API(쇼핑몰 API, LLM API)를 반드시 연동**합니다. 목업/더미 데이터는 사용하지 않습니다. 아래 범위 밖의 작업은 하지 마세요.

### 이번 단계에서 만드는 것
- 텍스트 자연어 검색 입력 → 의도 분해(카테고리/특징/제약사항, LLM API 실호출) → 실제 쇼핑몰 API로 상품 조회/매칭 → 카드 결과 표시
- 매칭 점수, 추천 이유, 가격 비교(실제 API 응답 기준), 구매 사이트 바로가기(실제 상품 딥링크)
- 필요한 API 키/크레덴셜 목록을 먼저 정리하고, 없는 키는 사용자에게 요청할 것 (임의로 목업으로 대체하지 말 것)

### 이번 단계에서 하지 않는 것 (범위 제외)
- 이미지 검색(Visual Search)
- Pinecone/Milvus 등 실제 벡터 DB 연동 (필요 시 인메모리 유사도 계산으로 대체)
- 결제, 로그인/회원가입, 관리자 페이지
- 실제 과금/제휴 정산 로직

> 새 기능 요청이 위 범위를 벗어나면, 목업으로 임시 처리하지 말고 필요한 API 키/크레덴셜을 먼저 사용자에게 요청한 뒤 실제 연동으로 구현합니다.

---

## 3. 기술 스택 (고정)
- **백엔드**: Node.js (Express) — `server.js` 단일 진입점 선호, 과도한 프레임워크 도입 금지
- **프런트엔드**: 정적 파일 (`index.html`, `app.js`, `styles.css`) — 별도 프레임워크(React 등) 도입 전에는 사용자에게 먼저 확인
- **LLM 연동**: Azure OpenAI 실호출 필수. `.env`의 `AZURE_OPENAI_API_KEY`, `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_DEPLOYMENT`, `AZURE_OPENAI_API_VERSION`으로 관리하며, 키가 없으면 작업을 진행하지 말고 사용자에게 키 등록을 요청할 것
- **쇼핑몰 데이터 연동**: 네이버쇼핑은 API 키 발급 불가/스크래핑 차단(HTTP 418)으로 사용하지 않음. 대신 다나와(Danawa) 검색 결과 페이지를 `services/scraper.js`에서 실시간 스크래핑(`cheerio`)해서 사용. 목업 데이터로 대체 금지, 사이트 구조 변경 시 셀렉터 갱신 필요

---

## 4. 코딩 규칙
- 기존 파일이 있으면 새로 만들지 말고 수정할 것
- 요청받지 않은 리팩터링, 과도한 추상화, 불필요한 헬퍼 함수 생성 금지
- 주석은 코드로 설명이 안 되는 부분에만 한 줄로 작성 (긴 설명 주석 금지)
- 민감정보(API 키 등)는 절대 코드에 하드코딩하지 않고 `.env` + `.gitignore` 처리
- 사용자 입력값은 항상 검증 후 사용 (XSS/인젝션 방지, 서버에서 최종 검증)
- 외부 API 실패 시 목업 데이터로 대체하지 말고, 에러 상태를 사용자에게 명확히 알리고 재시도/재검색을 유도할 것

---

## 5. 작업 방식
- 각 기능은 작게 나눠서 구현 → 바로 실행/테스트 가능한 상태 유지
- 코드 변경 후에는 실행 가능 여부(서버 구동, 에러 없음)를 확인하고 보고
- 요구사항이 모호하면 [prd.md](./prd.md)의 관련 섹션을 우선 기준으로 판단하고, 그래도 애매하면 가장 단순한 해석으로 진행 후 사용자에게 짧게 보고
- 보고는 "무엇을 했는지 / 왜 / 결과"를 짧게 한국어로 설명 (중학생도 이해할 수준)

---

## 6. 참고 문서
- 상세 기획: [prd.md](./prd.md)
